# ============================================================
# BookFlow — Environment Variables
# Copy to .env.local and fill with your values.
# ============================================================

# -----------------------------------------------------------------------------
# Supabase (required)
# Get these from: https://app.supabase.com → Project Settings → API
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# For migrations (scripts/migrate.js, CI): Supabase Dashboard → Project Settings → Database → Connection string (URI)
# DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# -----------------------------------------------------------------------------
# Razorpay (required for payments)
# Get from: https://dashboard.razorpay.com → Settings → API Keys
# -----------------------------------------------------------------------------
RAZORPAY_KEY_ID=rzp_xxx
RAZORPAY_KEY_SECRET=your-secret
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_xxx
RAZORPAY_WEBHOOK_SECRET=whsec_xxx

# -----------------------------------------------------------------------------
# Meta WhatsApp Cloud API (required for WhatsApp)
# Get from: https://developers.facebook.com → WhatsApp → API Setup
# -----------------------------------------------------------------------------
META_WHATSAPP_TOKEN=your-meta-whatsapp-token
META_PHONE_ID=your-phone-number-id

# Meta requires template messages for proactive sends. Default: hello_world (pre-approved).
# For custom reminders, create a template in WhatsApp Manager with params {{1}}={{2}}... and set:
# META_TEMPLATE_NAME=your_template_name
# META_TEMPLATE_LANGUAGE=en_US

# Optional: for webhook verification (Meta Developer Console)
# META_WEBHOOK_VERIFY_TOKEN=bookflow-verify

# -----------------------------------------------------------------------------
# App
# Use your production URL in production
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Admin: Re-apply template (fix UI when industry_type/custom_config mismatch)
# Generate a random string, e.g: openssl rand -hex 32
# -----------------------------------------------------------------------------
# ADMIN_SECRET=your-random-secret

# -----------------------------------------------------------------------------
# Upstash QStash (scheduled reminders — replaces Vercel Cron on free tier)
# Get from: https://console.upstash.com/qstash
# -----------------------------------------------------------------------------
QSTASH_URL=https://qstash-eu-central-1.upstash.io
QSTASH_TOKEN=your-qstash-token
QSTASH_CURRENT_SIGNING_KEY=sig_xxx
QSTASH_NEXT_SIGNING_KEY=sig_xxx

# -----------------------------------------------------------------------------
# Optional: Firebase Cloud Messaging (push notifications)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_FCM_VAPID_KEY=your-vapid-key
